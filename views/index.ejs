<!DOCTYPE html>
<html>
  <head>
    <title>Home</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <%- include('navbar', {links: links}) %>
    <h1>The Board of Messages</h1>
    <div class="board">
        <ul>
            <% for( let i = messages.length - 1; i >= 0; i++) { %>
            <%- include('messages', {message: messages[i]}); %>
            <% } %>
        </ul>
    </div>
  </body>
</html>